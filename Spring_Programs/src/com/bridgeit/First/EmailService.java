package com.bridgeit.First;

public class EmailService {
	public void emailService(String message, String receiver) {
		System.out.println("email sent to " + receiver + " with the message " + message);
	}
}
